import { Callout } from "react-native-maps";
import React, { useState, useEffect, Component } from "react";
import { View, StyleSheet, Image, Text } from "react-native";

export default ProfileScreen = () => {
  return (
    <Callout>
      <Text style={{ fontSize: 59 }}>Hi</Text>
    </Callout>
  );
};
